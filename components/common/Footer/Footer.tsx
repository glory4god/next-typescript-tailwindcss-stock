import { FC } from 'react';
import cn from 'classnames';

interface Props {
  className?: string;
}

const Footer: FC<Props> = ({ className }) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default Footer;
